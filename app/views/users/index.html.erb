<% provide(:title, 'All users') %>
<div class="container-narrow">
	<% if can? :new, Jobapp%>
	<h1>All users</h1>
	<%= will_paginate %>
	<ul class="users">
		<% @users.each do |user| %>
		<%= render user %>
		<% end %>
	</ul>
	<%=will_paginate%>
	
	<% end %>
</div>
